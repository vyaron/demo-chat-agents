---
description: Canonical agent instructions for this repo
alwaysApply: true
---

# Cursor Rules

The canonical agent instructions live in [AGENTS.md](../../AGENTS.md). Follow them.

This file exists only so Cursor auto-discovers the shared rules; do not duplicate
rule content here. When a convention changes, edit `AGENTS.md` (or the relevant
file under `.claude/rules` and `.doc/`), never this pointer.
