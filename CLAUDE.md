# CLAUDE.md

`AGENTS.md` is the source of truth for rules, guardrails, and repository layout in this
project. It is imported here so its content — and the `.claude/rules/` files it imports —
load into every session.

@AGENTS.md
