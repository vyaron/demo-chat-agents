# UI Rules

## Purpose
- Define default UI libraries for common interface elements.

## Library Choices
- Use `sonner` for toast messages.
- Use `lucide-react` for icons.

## Usage Notes
- Keep notifications concise and action-oriented.
- Reuse icon names consistently across similar features.